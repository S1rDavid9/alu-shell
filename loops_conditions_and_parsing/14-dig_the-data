#!/usr/bin/env bash
# This script groups visitors by IP and HTTP status code, and displays the data in the required format

# Use awk to process the TSV log, sort, and display the required format
awk -F'\t' '{print $1, $3}' log_file.tsv | sort | uniq -c | sort -rn | awk '{print $1, $2, $3}'
